# Facial-Expression-Detector
This repository contains a facial expression detection model build using Keras API on FER 2013 dataset. Along with the model there's a flask web application using which one can put host this locally for real time predictions 
